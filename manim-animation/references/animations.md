# Animations



- Prefer `ReplacementTransform` when the source mobject should not remain visible.

- Chain `.animate` calls in a single expression per `self.play()` argument.

- Use `FadeOut(Group(...))` to remove related mobjects together.

